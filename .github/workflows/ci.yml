name: CI - Build and Test

on:
  workflow_dispatch:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      # Step 1: Checkout source code
      - name: Checkout code
        uses: actions/checkout@v4

      # Step 2: Set up Java
      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: maven

      # Step 3: Build and test with Maven
      - name: Build with Maven
        run: mvn -B clean verify

      # Step 4: Build Docker image
      - name: Build Docker image
        run: docker build -t user-service:${{ github.sha }} .

      # Optional Step 5: Push Docker image (if you want CI+CD)
      # Uncomment and configure this if you want to push images to a registry
      # - name: Login to DockerHub
      #   uses: docker/login-action@v3
      #   with:
      #     username: ${{ secrets.DOCKERHUB_USERNAME }}
      #     password: ${{ secrets.DOCKERHUB_TOKEN }}

      # - name: Push image to Docker Hub
      #   run: docker push mydockerhubusername/myapp:${{ github.sha }}
